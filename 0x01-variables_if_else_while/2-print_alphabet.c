#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always (Success)
 */

int main(void)
{
	char c;
	/*code goes here*/
	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
